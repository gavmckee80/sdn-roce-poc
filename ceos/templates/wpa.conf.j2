ctrl_interface=/run/wpa_supplicant
ap_scan=0
network={
    key_mgmt=IEEE8021X
    eap={{ wpa_eap }}
    identity="{{ wpa_identity }}"
    password="{{ wpa_password }}"
    phase2="{{ wpa_phase2 }}"
    eapol_flags=0
}